import { PhaseGate } from '@/components/phase-gate';
export default function Login() {
  return <PhaseGate title="Team access" description="Role-based demo authentication arrives in Phase 3. Seeded role fixtures are not login accounts, and this page does not collect credentials." />;
}
