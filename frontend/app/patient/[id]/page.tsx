import { PhaseGate } from '@/components/phase-gate';
export default function Patient() {
  return <PhaseGate title="Patient review" description="Structured notes, field provenance, tracked edits and version-bound professional sign-off become available with the processing and review phases." />;
}
