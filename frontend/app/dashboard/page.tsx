import { PhaseGate } from '@/components/phase-gate';
export default function Dashboard() {
  return <PhaseGate title="Human-review queue" description="Phase 3 adds a role-controlled queue, urgency explanations, reviewer actions and escalation tracking. No priority has been calculated for the seeded fixtures." />;
}
