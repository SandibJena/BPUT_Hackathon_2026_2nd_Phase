import { PhaseGate } from '@/components/phase-gate';
export default function Admin() {
  return <PhaseGate title="Responsible-AI workspace" description="Later phases add authenticated audit verification, retention controls and measured evaluation results. Administrative actions are disabled until those controls exist." />;
}
