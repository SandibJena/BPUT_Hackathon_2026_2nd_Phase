# Local synthetic prototype only. No provider keys required in Phase 1.
APP_ENV=development
# Blank uses backend/triage.db (absolute path calculated by the backend).
DATABASE_URL=
FRONTEND_ORIGIN=http://localhost:3000
